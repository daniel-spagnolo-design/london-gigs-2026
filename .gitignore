# ── Secrets & personal data (NEVER commit) ───────────────────────────────
keys_local.py
Liked_Songs.csv
liked_artists.txt   # CI fallback artist list; lives only in the LIKED_ARTISTS secret

# ── Generated output / logs ──────────────────────────────────────────────
*.log
run_log.txt

# ── AI-tool / editor skill folders (not part of the project) ─────────────
.kiro/
.cursor/
.gemini/
.agents/
.claude/

# ── Local publish staging (e.g. Netlify) ─────────────────────────────────
publish/

# ── Python / macOS cruft ─────────────────────────────────────────────────
__pycache__/
*.pyc
.DS_Store
